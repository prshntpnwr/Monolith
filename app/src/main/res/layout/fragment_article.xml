<?xml version="1.0" encoding="utf-8"?>

<android.support.v4.widget.SwipeRefreshLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/swipe_refresh_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    android:layout_gravity="center_horizontal">

    <android.support.v7.widget.RecyclerView
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/recycler_view_article"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:paddingLeft="@dimen/material_cards_padding"
        android:paddingRight="@dimen/material_cards_padding"
        android:paddingBottom="@dimen/material_cards_padding"
        android:paddingTop="@dimen/material_cards_padding"/>

    <include
        android:id="@+id/empty_include"
        layout="@layout/empty_layout" />

</android.support.v4.widget.SwipeRefreshLayout>

